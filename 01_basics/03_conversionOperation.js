let score=33
console.log(typeof score);
console.log(typeof (score))

//33=>33(after conversion it gives back the number)
//33abc =>NAN(after conversion it does not gives the number back)
//true=>1;false=>0(values are converted to 0 and 1)
let someNumber=33

let stringNumber=string(soemNumber)
